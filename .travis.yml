sudo: false
language: node_js

cache:
  directories:
    - node_modules

before_install:
  - npm install npm@latest -g

notifications:
  email:
    on_success: never
    on_failure: change

install:
  - npm install

script:
  - npm test

branches:
  only:
    - master